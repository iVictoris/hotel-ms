{
    "name": "hotel-api",
    "version": "0.0.1",
    "description": "an api for my front end project, a hotel management system",
    "repository": "https://github.com/iVictoris/hotel-ms/server",
    "author": "ivictoris",
    "license": "MIT",
    "dependencies": {
        "express": "^4.17.1"
    },
    "main": "dist/index.js",
    "scripts": {
        "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
        "build": "tsc",
        "prestart": "npm run build",
        "start": "node .",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "@types/express": "^4.17.6",
        "@types/node": "^13.11.1",
        "tslint": "^6.1.1",
        "typescript": "^3.8.3"
    }
}
